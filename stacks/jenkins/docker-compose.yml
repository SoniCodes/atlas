services:
  jenkins:
    build: .
    image: atlas/jenkins:lts
    container_name: jenkins
    restart: unless-stopped
    ports:
      - "127.0.0.1:8081:8080"
    volumes:
      - jenkins_home:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock
    group_add:
      - "988"

volumes:
  jenkins_home:

