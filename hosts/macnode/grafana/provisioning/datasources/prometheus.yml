apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus
    access: proxy
    url: http://host.containers.internal:9090
    isDefault: true
    jsonData:
      timeInterval: 15s
